{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "install:lambda": "npm install --prefix lambda",
    "install:all": "npm install && npm run install:lambda"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.835.0",
    "dotenv": "^16.5.0"
  }
}
